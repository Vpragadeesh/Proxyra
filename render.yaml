services:
  - type: web
    name: proxyra-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: ROCKET_ADDRESS
        value: 0.0.0.0
      - key: PORT
        value: 10000
